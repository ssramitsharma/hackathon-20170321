#!/usr/bin/env python

import rospy

def main():
	rospy.init_node('controller')
	rate = rospy.Rate(10)
	while not rospy.is_shutdown():
		rospy.loginfo("Running the node, time {}".format(rospy.get_time())
		rate.sleep()
	pass

if __name__ = '__main__':
	main()
